defaults:
  - default

path: tiiuae/Falcon3-7B-Instruct
type: CausalLM
path_to_load_script: model/default_causal.py

instruct: true

load_model_args:
  device_map: auto
  # SDPA and vLLM do not return the attention probabilities needed by
  # GreedyProbsCalculator.
  attn_implementation: eager
load_tokenizer_args: {}
